파이썬에서 두 변수가 가리키는 메모리 주소가같으면 서로의 에일리어스(Alias)라고 말한다.
문자열처럼 그 자체의 값이 변경 불가능한 값은 에일리어스를 생성해도 안전하지만,
리스트(List)같은 가변적인 값은 에일리어스를 생성하게 되면 프로그램의 흐림이 개발자의 의도와 다르게 흘러갈 수 있다.

만약
list1 = [1, 2, 3, 4, 5]
list2 = list1
이라고 한다면 list1과 list2는 같은 리스트를 참조하고 있다는 뜻이 된다.
그래서 
list2[0] = 3
으로 바꾸게 되면 list1과 list2 모두
[3, 2, 3, 4, 5]
의 리스트를 가지게 된다.

이 문제를 해결하기 위해서는
list2 = list(list1) 이라고 지정을 해줘야 한다.